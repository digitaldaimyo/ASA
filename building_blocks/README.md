# building_blocks archive

This folder contains the original research notebooks and other raw artifacts as
an immutable archive. Do not edit the contents; the public API and runnable
scripts live in the top-level `src/`, `scripts/`, `tests/`, and `docs/` folders.
